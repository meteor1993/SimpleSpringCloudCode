server:
  port: 9000
spring:
  application:
    name: spring-cloud-hystrix-dashboard
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: "*"
logging:
  level:
    org.springframework.*: debug