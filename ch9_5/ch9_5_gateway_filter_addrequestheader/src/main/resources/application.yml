server:
  port: 8080
spring:
  application:
    name: spring-cloud-gateway-filter-addrequest
  cloud:
    gateway:
      routes:
      - id: addrequest_filter
        uri: http://localhost:8081
        predicates:
        - Path= /hello/{segment}
        filters:
        - AddRequestHeader= X-Request-hello, alpaca
